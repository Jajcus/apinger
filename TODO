- remove hardcoded paths (eg. to the config file)
- don't fire "loss" alarm when "down" alarm is on
- write state information, so current alarm state can be watched eg. via WWW
- rrdtool statistics generation support
